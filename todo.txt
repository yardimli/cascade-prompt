DONE:

- replace contenteditable for cells with a an input textarea that starts in size of the cell but grows vertically and horizontally as needed
- make the cell input overflow:hidden and add a tooltip that shows the full content on hover if the content is larger than the cell so the content of the cell wont effect the table layout

- on the top menu add a merge and unmerge button with icons
- allow user to merge selected cells using menu buttons. use colspan and rowspan attributes to achieve this, if cells contain data merge the data with a separator (e.g. space or comma)
- allow user to unmerge merged cells using a context menu, when unmerging keep all data in the top-left cell of the merged area and clear the rest. unmerge button should only work if a merged cell is selected

- create a new javascript file to keep track of the data in the tables, it should have functions to get/set cell data that is called from the other scrips. load and save functions to convert the table data to/from a json structure should also be added here.
- add array structure to store multiple sheets within a project, only store cells with content along with the height and width of rows and columns, store merged cell info (start row/col, colspan, rowspan) and total number of rows and columns for each sheet
- add tab at the bottom allow user to create new sheets and navigate between existing sheets.

- add php backend to save/load projects (save as json to projects folder) and load from there, create a modal to manage projects (load/delete) and to create new projects and save current project (first time) or save as...
- add a top menu bar with file/edit/view/help main menus and relevant submenus, create placeholders for future functionality
- remove the localstorage functionality, only remember the last open file in localstorage and open it on page load if available

- save cursor position and highlighted cells when switching sheets and restore them when returning to the sheet also save them to the project file and restore them when loading a project

- improve UI (menus)
- add light/dark mode toggle in the view menu and remember user preference in localstorage

- implement undo/redo functionality for cell edits, merges/unmerges, row/column resizing, and sheet changes. maintain a history stack to track changes and allow users to revert or reapply actions. create a new history javascript file to manage this functionality.

- allow user to drag the selected cells to a new location. merged cells should be moved as a whole. overwrite any existing cells in the target location. clear the original cells after the move.

- make html formatting buttons work.
- add cell border, color and background color options in the top menu for better cell customization, apply to cursor or selected cells.

- improve rendering and performance for ui and file operations.

- add copy/paste functionality for cells and ranges of cells using keyboard shortcuts (Ctrl+C, Ctrl+V). ensure that formatting and merged cell states are preserved during copy/paste operations. add copy, paste, cut buttons to the top toolbar and make the menu items in the edit menu functional.


- add project llm settings dialog where user can input openrouter api key. it should be saved in the project json file. add menu item to the file menu to open the dialog.

- add button to toolbar and file menu item to create a =llm() formula, it should open a dialog where user can input prompt (textarea) select language model to use (add refresh button besides the llm model dropdown to get all models from openrouter, save model list to localstorage).
In the prompt user can use #A-17 to reference other cells which will be replaced with the content of the cell when sending the prompt to the llm.
Add textarea where user inputs json structure of results expect (as a table key: value pairs) so the response can be parsed correctly.
the json structure should be added to the prompt so the llm knows what to respond with.
add cell coordinates where result should be inserted.
the json should be parsed an inserted into the specified area as rows and columns.
add a button to the cell when user clicks on the button the llm formula is exectued and the result is inserted into the sheet.
- the llm functions should be a new javascript file.

- in the llm dialog make target cell selection use validation dont allow user to type invalid cell coordinates
- in the llm dialog validate the json structure input and show error if invalid
- make the hint (.form-text) work with darkmode
- on the sheet when formula cell is selected the formula-input should show the mouse cursor as a pointer instead of disabled.
- in the llm prompt editor when user enters #A17 make that part highlighted to indicate its a cell reference with preview of the cell content on hover.
- in the llm prompt editor allow user to enter cell ranges like #A17:B17 and highlight that part with preview of the cell contents on hover and show content and number of cells in the range.


- allow filter in llm dropdown to search for models by name. use select2.js library for the dropdown.

- double clicking on a field should adjust the textarea to fit the content not wait for a keyboard input.

- add dropdown option to the sheet with formula (=dropdown("options","selection")) that allows user to create a dropdown in the cell with specified options and save the selected option in the cell data. when used in the llm prompt the selected option should be used as the cell content.

- make overlay editor for cell content have max-width and max-height and fit the screen and grow in height show scrollbar if needed.

- add sheet properties dialog where user can set number of rows and columns, and name of sheet it should be alphanumeric with no spaces so it can be used in formulas. add menu item to edit menu to open the dialog. Default sheet names should be unique and check for existing names when creating or renaming sheets.

- add fal.ai api key to the llm settings dialog.

- in llm dialog add checkbox to continue adding results at the end instead of overwriting specified target area.

- for the preparation of supporting image input when making llm call, remove key "html" and "text" with the new key "type". Key "type" is for the cell has "text" or "number" or "dropdown" or "llm_formula" or "checkbox" or "datetime". Implementation doesn't have backward compatibility and the project json is ignored, so need to create new file for testing.
example of "text"->
"0-1": {
          "type": {
            "name": "text",
            "details": {
              "value": ""
            }
          },
          "rowspan": 1,
          "colspan": 1,
          "style": {
            "cssText": "color:oklch(0.21 0.006 285.885);font-weight:400;font-style:normal;font-size:16px;text-align:start;"
          },
          "cellStyle": {}
        }
example of "number"->
"2-5": {
          "type": {
            "name": "number",
            "details": {
              "value": "10"
            }
          },
          "rowspan": 1,
          "colspan": 1,
          "style": {
            "cssText": "color:oklch(0.21 0.006 285.885);font-weight:400;font-style:normal;font-size:16px;text-align:start;"
          },
          "cellStyle": {}
        }
example of dropdown->
"3-0": {
          "type": {
            "name": "dropdown",
            "details": {
              "options": [
                "Fantasy",
                "Adventure",
                "Science Fiction",
                "Nonfiction"
              ],
              "selected": "Nonfiction"
            }
          },
          "rowspan": 1,
          "colspan": 1,
          "style": {},
          "cellStyle": {}
        }
example of button with llm formula->
"0-3": {
          "type": {
            "name": "llm_formula",
            "details": {
              "prompt": "Create 3 book titles and hooks(maximum 255 characters) from the subject in #A2 for the target age specified in #A4",
              "model": "openai/gpt-5.2",
              "jsonSchema": "{\n  \"Title\": \"String\",\n  \"SubTitle\":\"String\"\n}",
              "targetRow": 2,
              "targetCol": 3,
              "funcName": "Generate Title and SubTitle",
              "includeHeaders": false,
              "insertMode": "overwrite",
              "component": "button"
            }
          },
          "rowspan": 1,
          "colspan": 1,
          "style": {},
          "cellStyle": {}
        }
- allow user to add image to a cell from url. images should resize to fit the cell but keep aspect ratio.
- refactor javascript files based on their tasks and purpose
- allow user to add image to a cell from local file. images should resize to fit the cell but keep aspect ratio.(Only for node now, new package is required $npm install multer)
- improve the cell reference tag's editing part in the prompt input of the Insert LLM formula dialog.
- create a generic property panel area. First step: moved the dropdown form to the property panel. The panel title is Formula Editor.
- move LLM formula form dialog to the property panel(Formula Editor) like the doprdown form.
- make the llm call support image inputs so user can reference images in the sheet when using llm formula. images should be added in a new input type(Image Attachments). User will add reference cell tag that contain images. The images are uploaded are added as base64 data urls to the llm request. Any reference cell tag doesn't contain image will be ignored. Image Attachments input accept maximum 5 images.
- add checkbox (with its values configurable), Checkbox Label is optional.

TODO:
- delete to clear the content of the cell.
- apply the text style to the entire content of the cell instead.

- update prompt dialog so cell references (#A17) is current sheet and allow user to select cell references from other sheets using sheetname!A17 format.
- in llm trigger form, change the Expected JSON Structure to key:value input

- add =folderreader("folderpath") formula that reads all files in a folder and can feed them to the llm one file at a time
- add =filereader("filepath") formula that reads a file content and feeds it to the llm one line at a time
- =folerreader and =filereader should also work with full output


- allow user to create =imagegen() formula that uses fal.ai to generate images based on a prompt from the sheet cell content.


--------- BUGS:
- [v] In llm trigger form, the reference cell tag preview isn't updating if the cell content is changed.

- [v] when resizing a selected area, the highlighted border is not following the border of the original cells.
- [v] selecting single cell is not working, also after making a selection making it smaller is not working correctly.
- [v] moving cells is not working including moving single cell. once selection is made the mouse cursor should change to drag when over the selected area to indicate its draggable.
- [v] when unmerging cells the new cells are not editable until page refresh
- [v] after mering cells, its highlihgted background is wrong(only at the size before it gets merged), and its border is still shown as the original size.


- [v] Create new file is not working correctly. It opened blank without grid at first, refresh it then the grid appears.
- [v] Open project -> delete current project -> cancel -> still see the currect project open -> yes I can save it

- [v] new sheets row heights are different than the first sheet, should have some default height. However the new sheets' single row height is better because the default Sheet 1 row height cut of the bottom of the text and the cell-editor height sticks out. -> the centralized default height is set by a single property "SheetDataManager.defaults.defaultRowHeight" now.

- [v] when merging cells row height resize acts weirdly for the rows that intersect with merged cells
- [v] When merging cells, if more than one cell has value, it causes problems. ->only keep the value of the cell at most top left location.
- [v] The cursor on text and number cell should be the “point” until it becomes in editing mode.

- [] cell content become empty or replaced when switching between sheets when the cell is in the edit mode. start editing a cell A8 on sheet1 and leave it in the edit mode->switch to sheet2->switch back to sheet1->either the Sheet1!A8 become empty or the content of sheet1A8 appear on sheet2!A8 instead(whole content of columns can be affected)

- [] the "text color" and "fill color" is buggy.
- [] border tool is not working correctly with cells merged and unmerged.
- [] the editing from formula bar input for text cell and number cell is buggy.


- [] Can’t change the cell texts or number from the formula input
- [] In llm trigger form, the prompt input can’t handle texts pasted into it
- [] In llm trigger form, the output cell can be the same as the target cell, it causes problem.

-------- FEATURE QUESTIONS:
when using llm button, can I control the output to be displayed by rows or by columns?
